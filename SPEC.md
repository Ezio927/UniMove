# UniMove 项目规约

## 1. 项目概述

UniMove 是面向校园体育活动的全栈 Web 应用。学生可以浏览与检索活动、报名或取消报名、管理订单、发表评论并维护个人资料；管理员可以创建和维护活动。本课程项目选择 B 类“应用类项目”，不构建 Agent Harness。

本项目以已有 UniMove 仓库为代码基线。课程范围聚焦于工程化整理、自动化测试、容器化交付，以及一个按 Superpowers 流程开发的“活动收藏”增量。

## 2. 目标与成功标准

- 提供可访问的中文 WebUI，覆盖注册登录、活动浏览、活动详情、报名、评论和个人中心。
- 前后端职责分离，业务规则集中在服务层，HTTP 错误统一处理。
- 核心功能具有可重复运行的自动化测试和 CI。
- 不提交真实凭据，敏感配置只通过环境变量提供。
- 支持 Docker Compose 启动，并提供清晰的本地运行与分发文档。
- 完成一个可追踪的活动收藏增量，保留 SPEC、PLAN、TDD、subagent 和评审证据。

## 3. 用户与核心场景

### 普通用户

- 注册、登录并编辑个人资料。
- 按关键词、类别、地点、价格和时间浏览活动。
- 查看活动详情并报名或取消报名。
- 查看订单、评价参加过的活动。
- 收藏、取消收藏活动，并在个人中心查看收藏列表。

### 管理员

- 登录后创建活动。
- 维护活动的基本信息与状态。

## 4. 功能模块

1. **认证与用户模块**：注册、登录、JWT 鉴权、资料维护和密码修改。
2. **活动模块**：活动创建、检索、筛选、详情和状态管理。
3. **报名与订单模块**：原子名额占用、订单状态转换和取消补偿。
4. **评论模块**：评论增删改查、评分与统计。
5. **活动收藏模块（课程增量）**：登录用户收藏、取消收藏和查看收藏活动。

## 5. 活动收藏增量

### 功能约束

- 用户文档通过 `favoriteActivities` ObjectId 数组保存收藏。
- 收藏使用 `$addToSet`，取消使用 `$pull`，重复请求保持幂等。
- 收藏不存在的活动返回 404；非法活动 ID 返回 400。
- 被物理删除、无法再由数据库查到的活动不出现在收藏列表；`cancelled` 和 `completed` 活动仍可作为历史收藏显示。
- 所有收藏接口都必须经过现有 JWT 认证。
- 活动列表、活动详情与个人中心提供收藏交互。
- 不增加收藏夹分类、通知、推荐或未登录本地收藏。

### API

- `GET /api/users/favorites`
- `PUT /api/users/favorites/:activityId`
- `DELETE /api/users/favorites/:activityId`

三个接口成功状态码均为 200。GET 返回 `data.activities`；PUT 和 DELETE 返回
`data.favoriteActivityIds`。PUT 消息为“收藏成功”，DELETE 消息为“已取消收藏”。
DELETE 对未收藏或已经删除的活动仍然成功，以保证幂等。

收藏列表不分页，按照活动 `createdAt` 倒序返回。本次只在读取时过滤不存在的活动引用，不扫描并清理用户文档中的历史 ID。

## 6. 非功能要求

- Node.js 版本不低于 22.12.0。
- 前后端均通过 ESLint、TypeScript 类型检查、Vitest 和生产构建。
- 根目录提供一条统一质量验证命令。
- CI 至少执行依赖安装、类型检查和全部单元测试。
- 不增加无必要的运行时依赖。
- 移动端和桌面端均应保持基本可用。

## 7. 安全边界与威胁模型

- `JWT_SECRET`、MongoDB 密码和管理界面密码不得进入 Git 历史。
- JWT 只用于身份认证；服务端不能接受客户端指定的其他用户 ID 来修改收藏、订单或资料。
- 请求体通过 Zod 严格校验，资源 ID 在进入数据库操作前验证。
- 密码使用 bcrypt 哈希；HTTP 使用 Helmet、CORS 和速率限制。
- Mongo Express 仅用于本地管理，必须启用单独的基本认证，不作为公开产品界面。
- 本项目不使用付费第三方 API，课程要求中的第三方 API 凭据生命周期不适用。

## 8. 技术选型

- 前端：React 19、TypeScript、Vite、Ant Design、Redux Toolkit。
- 后端：Node.js、Express、TypeScript、Mongoose、MongoDB。
- 测试：Vitest、Testing Library。
- CI：GitHub Actions 与课程指定 GitLab CI。
- 分发：Dockerfile 与 Docker Compose。

界面沿用既有 Ant Design 设计系统，采用简洁的蓝色校园应用风格。本课程增量不更换现有设计系统，避免与已有页面产生视觉割裂。课程强烈推荐但未强制要求的 Open Design 未用于早期基线；该偏离在 `AGENT_LOG.md` 中如实记录。

## 9. 验收方式

- 在干净环境安装依赖后，前后端测试、类型检查与构建全部通过。
- 使用 Docker Compose 启动 MongoDB、后端和前端。
- 使用普通账号完成活动浏览、收藏、取消收藏和个人中心查看收藏的流程。
- 未登录访问收藏接口得到 401，非法 ID 得到 400，不存在活动得到 404。
- CI 最后一次流水线通过，README 提供运行、分发、安全边界和已知限制说明。
