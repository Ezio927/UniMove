# UniMove 课程项目反思：在证据与效率之间取舍

在 UniMove 的开发和提交准备中，我最明显的收获不是某一项单独的功能，而是对“完成”应当如何被确认有了更具体的认识。Superpowers 是一套把设计、测试、评审和验证串起来的工作方法；其中 `verification-before-completion` 要求在宣称完成前拿出可复现的证据，`systematic-debugging` 则要求先定位问题、再排除猜测。这两项对我最有帮助，因为它们减少了凭感觉宣布完成和遇到异常就直接猜原因的情况。MongoDB URI 的协议格式、Atlas 的 IP allowlist 配置，都让我看到部署并不是“服务启动了”就算结束；需要逐项确认连接字符串、网络访问和实际运行结果。浏览器中的 Dropdown/Select 弹出层问题也留下了反例：目前真实浏览器里的弹出交互仍不可用，不能因为局部检查通过就把它说成已经修复。

测试驱动开发（TDD）在本项目中总体利大于弊。它强调先为预期行为写测试，再补齐实现，我把它用在收藏、认证、健康检查和部署契约上，能较早明确接口和失败边界，也让回归检查有据可依。不过 Dropdown/Select 说明了它的边界：jsdom 环境中的状态和 DOM 测试可以通过，却没有验证用户能否在真实浏览器看到并使用弹出的内容。测试覆盖了状态，不等于覆盖了可见几何和真实交互。因此，TDD 不能取代浏览器端到端测试；对这类依赖浏览器表现的组件，真实浏览器验证应更早进入流程。

我也体会到流程严格程度需要和风险相称。多次分派 subagent、记录模型和提示词、反复审阅文档，能留下可追溯的过程，尤其在需要交接或复核时有价值；但对小型、低风险的课程任务，有时形式成本超过直接收益。主 Agent 在需要持续整合上下文和连续决策时更快，因为不必反复装载背景；subagent 则更适合边界清楚的实现或独立评审，但会付出重复理解上下文的代价。对我而言，理想的 subagent 任务应限定为一个功能、少量文件、清晰接口、可独立执行的测试，并在一次会话内形成一个可审阅的提交。这样既保留分工带来的检查，也避免把协作本身变成额外负担。

这种取舍并不是反对记录或复核，而是要求我先判断风险在哪里。涉及认证、收藏、健康检查、部署契约和外部服务配置时，错误会影响核心行为或可交付性，测试、审查和明确证据值得投入；涉及很小的文档或本地改动时，仍应保证内容准确，却未必需要相同层层展开的派发和登记。持续的台账在跨轮次协作中有用，因为它能保留已经确认的范围、命令和结论；但台账也应服务于下一步判断，而不能替代判断本身。对一个清晰而独立的任务，集中完成、运行对应测试、提交可读 diff，往往比拆成许多彼此依赖的说明更合适。对需要整合大量已有决策的任务，由主 Agent 连续处理则能避免每一轮重新解释同一份背景。以后选择协作方式时，我会先把接口、文件范围和验证方式写清，再决定是否值得分派，而不是把分派当成默认步骤。

提交准备中的一次较大返工来自于我把 NJU GitLab 和大量线上业务冒烟检查当成硬性提交门槛。后来老师澄清，模板中的 GitHub 字段只是示例，实际应提供真实的仓库或部署地址。这个变化提醒我，在投入大量执行成本前，应先确认老师对交付物的接受标准，而不是从模板文字推导出更严格的要求。弹出层测试同样揭示了这一点：只验证状态变化，未验证可见的实际几何结果，就容易把不充分的信号当成通过。无论是提交规则还是测试信号，关键都在于确认它真正代表了要证明的事情。

这也影响了我对提示词和上下文的看法。有效的协作说明应明确工作区与文件范围、禁止动作、准确的输入输出、必须执行的命令，并配合聚焦的 diff 包和持续维护的台账。这样的上下文足以让接手者判断任务和验证结果；把无关的完整对话历史一并塞入，反而会稀释重点、增加理解成本。Superpowers 的价值正在于为设计、测试、评审和验证提供约束，使核心工作不容易漏掉；但如果机械套用，每个小改动都走完整流程，就会制造不必要的开销。今后我会把严格门槛保留给核心逻辑、凭据和部署，把文档或本地的小改动采用更轻的过程。

安全和交付方面的认识也更具体了。凭据应留在受控的仪表板或环境变量中，而不应进入仓库、日志或聊天记录；URI 中的特殊字符需要正确编码，Atlas 的网络 allowlist 也直接影响连接是否可用。Docker、GHCR、CI 以及可复现的拉取过程不是开发完成后的装饰，而是工程交付的一部分。它们与功能代码共同决定其他人能否获得同样的运行结果。若重新开始这个项目，我会更早确认老师接受的提交形式，更早加入真实浏览器端到端测试，并按风险调整流程强度。总体而言，Superpowers 值得使用，因为它促成设计、测试、评审和验证；但它应服务于可靠交付，而不应被当成不分场景的机械手续。

我会把“可复现”理解得更完整一些：它既包括测试能重复运行，也包括部署所依赖的镜像、持续集成和拉取路径能够被检查。连接服务时，协议格式、字符编码和网络规则必须同时成立，少一个条件都可能让表面上的配置失效；交付容器时，构建、发布和拉取能够衔接，才说明成果能被他人使用。这样的认识并不要求把每一次改动都扩大为复杂的线上检查，而是提醒我把验证放在最能暴露风险的位置。对于核心逻辑、凭据和部署，我宁愿保留清晰而严格的证据链；对于局部、低风险的变更，则以与风险相称的检查换取更直接的推进速度。这是我希望带到以后项目中的工作习惯。

*AI 协助披露：本文的九项实质观点均已由本人逐项审阅并确认；AI 仅协助组织结构和润色语言。*
